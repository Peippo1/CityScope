rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // The browser never reads or writes saved investigations directly. All
    // access is authorized by the FastAPI ownership boundary using Admin SDK.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
