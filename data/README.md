# Data artifacts

Generated Parquet artifacts live in `data/generated/` and are ignored by Git. The checked-in fixture and metadata are intentionally small and reproducible. Production data must include explicit licence, attribution, retrieval date, checksum, and observation-period metadata before publication.
